let scoore: number | string = 33;

type baags ={
	name : string
}

type cosst = {
	currency : string
}

let materisal : baags | cosst = {name:'popo'} 


//manipulathing data with union
function calculate (id : number | string){
	  id === "string" && id.toLowerCase  
}