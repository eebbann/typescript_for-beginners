const superHeroes :string[] = [];

superHeroes.push("pop","lol")

//this shows strictly only number or string values
const great: number [] | string []= [9,1,3,4,5]

//for more than one data type
const grean : (number | string )[] = ["pop", 1,3,"papa"]

//adding type to arrays
type usse ={
	name: string,
	gender: true
}

const namaste: usse[]=[]

namaste.push({name :"gril", gender: true})