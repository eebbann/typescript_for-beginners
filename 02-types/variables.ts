let greetings : string = "hello tasha";

greetings.toLocaleLowerCase()
console.log(greetings)

//THE PRIMITIVE TYPES

//-number :
let userID:number  = 0;
//note typesscript is smart enough to determine the number without adding :number to the variable
//eg : let userID = 23 (you can try this by removing the comment )
userID.toFixed()

//boolean : (true and false	) more like a yes and no answer

let isLoggedIn : boolean = false;






export {}