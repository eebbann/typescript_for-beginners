//enums are like objects but very stict in their values 
//adding const before enum makes it more readable when compiled to javascript 
const enum BodyPart { 
	 eye = 1,
	 NOSE = 2,
	 MOUTH
}

const manny = BodyPart.eye


export{}